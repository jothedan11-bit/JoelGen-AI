## Packages
(none needed - base stack includes framer-motion, lucide-react, hook-form, etc.)

## Notes
- Images are returned as Base64 data URIs in the `content` field.
- Dark mode is default.
- Using Framer Motion for animations.
